Change Log--Directory Syncher version 1.1

-BUGFIX: New exception handling for bad synchrc files now prints error message to std error.

-Progam will now convert given path names to canonical path names.
-Removed warning about the use of relative path names.
-Program will now use the older of two mod times when two identical files have different mod stamps.

